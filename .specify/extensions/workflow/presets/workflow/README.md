# Sanduq managed workflow preset

Install through `specify preset add --dev <path> --priority 1`. Requires the workflow extension and configured policy. Package builds bundle this canonical preset alongside scope-gate and scope-brainstorm. Do not edit installed command copies.
