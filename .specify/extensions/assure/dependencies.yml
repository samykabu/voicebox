schema_version: "1.0"
dependencies:
  - id: illustrate
    version: ">=2.0.0,<3.0.0"
    required_for: "diagram readiness analysis and manual visuals"
defaults:
  update_policy: prompt
  check_interval_hours: 24
