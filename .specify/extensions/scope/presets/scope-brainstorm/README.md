# Optional GitHub Brainstorm preset

The installer applies this prepend preset only when Superspec is installed and
enabled with its Brainstorm command available. It preserves that upstream command
as reference material while replacing its interactive transport with the shared
GitHub clarification skill. If Superspec is absent, automatic dispatch uses Clarify.
