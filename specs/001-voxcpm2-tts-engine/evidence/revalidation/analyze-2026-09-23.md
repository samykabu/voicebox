# Analyze revalidation, three passes (2026-09-23)

A read-only cross-artifact analysis of `specs/001-voxcpm2-tts-engine`. No tests were run, and this file is the only file written.

| Pass | Tree analysed | Result |
|---|---|---|
| 1 | `d211d1d` (all 56 tasks done) | 13 findings: H1, H2, M1–M6, L1–L5 |
| 2 | `4b72fed` (after `699f362` H1 code fix and `4b72fed` spec-folder docs) | All 13 resolved; 7 new: N1 (HIGH), N2–N7 (LOW) |
| 3 | `d0a0528` (after `39cdc3a` N4 test and `d0a0528` docs and specs) | N1–N7 resolved; 2 new: P1 (HIGH), P2 (LOW) |
| 4 | `90913de` (P1 and P2 fixes) | P1 and P2 resolved; branch-wide Style and constitution scan finds nothing new; nothing open |

## Pass 1: the original 13 findings (all resolved)

| ID | Sev | Finding (short) | Status | Verified at |
|---|---|---|---|---|
| H1 | HIGH | VoxCPM2 was refused on XPU/DirectML, contradicting FR-023's processor fallback | Resolved (`699f362`) | backend/backends/base.py:304-319; backend/tests/test_cpu_fallback.py; plan.md:51; data-model.md:78-89; engine-capabilities.md:72-74, 93 |
| H2 | HIGH | Data model and quickstart still described voice design with no profile | Resolved | data-model.md §3 and the relationships section; quickstart.md Step 6 |
| M1 | MED | The `options` keyword was described both as a protocol change and as no change | Resolved | tts-backend-protocol.md:6-9, :70; plan.md:48; matches backends/__init__.py:122-129 |
| M2 | MED | T025 said the model's encode rate; the code uses 24000 Hz | Resolved | tasks.md:131 |
| M3 | MED | Capabilities contract was out of date (status, cfg max 5.0, placeholders, open item) | Resolved | engine-capabilities.md:3, 47-57, 81, 121-126 |
| M4 | MED | plan.md not updated for T048–T056 or the T002 decisions | Resolved | plan.md:31-38, 47, 51, 55, 88-130, 182-190, 203-206 |
| M5 | MED | FR-025–027 missing from the data model and quickstart | Resolved | data-model.md §5-§7; quickstart.md Step 6b |
| M6 | MED | T002-pending note in tasks.md and FR-023 row in the checklist out of date | Resolved | tasks.md:289; checklists/requirements.md:44; spec.md:206 |
| L1 | LOW | spec status still "Draft" | Resolved | spec.md:7 |
| L2 | LOW | T009 missing `min_memory_mb`; dependency graph missing T048–T056 | Resolved | tasks.md:67, 240-245, 260-262 |
| L3 | LOW | `torchaudio` had no version floor | Resolved | backend/requirements.txt:58 `torchaudio>=2.5.0` |
| L4 | LOW | Key Entities did not describe designed profiles | Resolved (see N5) | spec.md:155-157 |
| L5 | LOW | No task covered two edge cases | Resolved as a note | tasks.md:291-295 |

## Pass 2: findings N1–N7 (all resolved)

| ID | Sev | Finding (short) | Status | Verified at |
|---|---|---|---|---|
| N1 | HIGH | README, CHANGELOG and developer docs still said VoxCPM2 is refused or greyed out on XPU/DirectML, with a stale "…or CPU…" reason | Resolved (`d0a0528`) | backend/README.md:151-152, :161; CHANGELOG.md:30-33, 57-63; model-management.mdx:41, 51, 154, 243-244; tts-generation.mdx:147, 174 (the reason now reads "needs CUDA or Apple Silicon (MPS)", as `_join_labels` builds it); tts-engines.mdx:227, 663, 687, 694, 696, 732; overview/gpu-acceleration.mdx XPU and DirectML sections. The grep below finds no remaining refusal claim. |
| N2 | LOW | T054 called `/speak` "non-persisted" | Resolved | tasks.md:194 names `generate_audio_sync` (no live caller) and says `/speak` and MCP speak save through `save_audio` |
| N3 | LOW | Principle V boxes unticked | Resolved | engine-capabilities.md:102-106, ticked with evidence. I checked the evidence: models.py:620/630/659, DefaultService.ts:2278, models.py:108-138, README.md:110-161 |
| N4 | LOW | No test for a failed second-engine load; processor out-of-memory not mentioned | Resolved | backend/tests/test_engine_load_failure.py (`39cdc3a`): the real `run_generation` and `load_engine_model` with fake backends. It asserts the generation is marked failed with the error, the failing engine stays unloaded, and the loaded engine generates again with no reload. tasks.md:295 notes that on a processor-only machine the OS may stop the process, and that no memory warning guards that case (memory is read only for CUDA; this corrects my pass-2 wording). |
| N5 | LOW | Code identifier in spec Key Entities | Resolved | spec.md:155 |
| N6 | LOW | T008 rule and the H1 change missing from tasks.md | Resolved | tasks.md:65 (processor-fallback row), :297 (H1 notes entry) |
| N7 | LOW | US3 test needed hardware that no longer exists | Resolved | spec.md:58 (injected configuration, test_cpu_fallback.py) |

## Pass 3: new findings on the current tree

| ID | Severity | Location | Finding | Suggested fix |
|---|---|---|---|---|
| P1 | **HIGH** (constitution) | backend/tests/test_engine_load_failure.py:18 (new in `39cdc3a`); backend/tests/test_audio_disclosure.py:10; backend/tests/test_generation_advanced_settings.py:14; backend/tests/test_voxcpm_backend.py:11 | These four test files are new with this feature, and each has `from __future__ import annotations`. The constitution's Style rule (constitution.md:151-153) and backend/STYLE_GUIDE.md:51 forbid it explicitly. ruff does not enforce it, which is why N4-ruff.txt passes. Mitigating context: 15 files already on `main` (upstream) use it, so this repeats existing practice, and the fix is mechanical. My earlier two passes missed the three older files. The fourth is new in this pass. | Delete the line in the four files, quote any forward reference that then fails, and re-run `just check-python` and `just test`. If the project would rather tolerate the upstream practice, amend the Style rule by the constitution's amendment procedure; don't leave it silently broken. |
| P2 | LOW | docs/content/docs/overview/generating-speech.mdx:27; docs/PROJECT_STATUS.md:701 | Both are incomplete rather than wrong. The overview says the download dialog shows a warning "if your graphics card looks short of memory"; on XPU/DirectML it shows the processor-fallback warning instead. PROJECT_STATUS mentions only "an advisory memory `warning`" and "only VoxCPM2 declares accelerators", which suggests VoxCPM2 can be greyed out or refused, when no registered engine can be today. | generating-speech.mdx:27: add "or, on an Intel Arc or DirectML machine, a note that it will run on the processor". PROJECT_STATUS.md:701: add the processor fallback and "no registered engine is refused today". |

### Leftover-claim grep (pass 3)

- **Searches:**
  - `(xpu|directml|intel arc)` together with `refus|unavailable|greyed|grayed|can't run|cannot run|400|not available`, over `docs/`, `backend/README.md` and `CHANGELOG.md`.
  - `voxcpm` together with the same terms, over the same files.
  - `greyed|grayed|can't run|cannot run` over `docs/content/docs/overview/` and the docs index.
  - `xpu|directml` over the spec folder (not evidence).
- **Result:** no remaining claim that VoxCPM2 is refused, unavailable or greyed out on XPU/DirectML.
  - The only XPU hits are README.md:161 and tts-generation.mdx:174. Both now say VoxCPM2 is *not* refused.
  - quickstart.md:96 and tasks.md:297 describe the fallback correctly.
  - The old CHANGELOG sections (320, 351, 360) are historical.
  - PROJECT_STATUS.md:701 is the only incomplete line (P2).

### Judgement: the two overview lines left unchanged

- `docs/content/docs/overview/generating-speech.mdx:25` ("An engine this computer can't run is greyed out, with a sentence saying why. It is not hidden.") and `docs/content/docs/overview/introduction.mdx:65` ("An engine your computer can't run is shown greyed out with the reason.") are **accurate**.
  - Both are conditional statements about a mechanism that still exists (backend/backends/base.py:321-336; app/src/lib/hooks/engineCapabilityRules.ts:18-40).
  - Neither says any engine is greyed out today, and neither mentions XPU or DirectML.
  - Neither needs to change. The one user-facing gap in that section is the dialog sentence at generating-speech.mdx:27 (P2), not line 25.

## Constitution

- **Principles I–V:** satisfied.
  - Offline load (T046/T047).
  - Principle II: the acknowledgement, the disclosure on every generated surface (untagged user audio verified at profiles.py:236-237, :624-625 and transcription.py:55-56), and export provenance.
  - The engine declares its accelerators and now degrades to the processor.
  - Principle V: obligations met and the README accurate after N1.
  - CI runner policy: release.yml runs-on is unchanged.
- **Style rule (Development Workflow):** broken by P1 in pass 3; fixed in `90913de`. The pass-4 scan finds no breach the branch introduced.

## Pass 4: P1 and P2, and a branch-wide scan (`90913de`)

### P1 and P2

| ID | Status | Verified at |
|---|---|---|
| P1 | Resolved | `90913de` removes `from __future__ import annotations` from the four test files. ruff check and format pass (P1-ruff.txt). The four files run 191 tests with 0 failures (P1-four-files.xml). The full suite runs 1073 tests with 1 failure and 9 skipped (P1-full-suite.xml); the one failure is `tests.test_progress::test_hf_progress_tracker`, the same failure already present before this work. |
| P2 | Resolved | generating-speech.mdx:27 now names both warnings: memory, and on XPU/DirectML the processor fallback. PROJECT_STATUS.md:701 names both warnings and says VoxCPM2 is never refused; the greyed-out state and the 400 remain for a future engine that declares no processor path. |

The remaining `from __future__` imports are in `backend/services/generation.py` and `backend/tests/test_all_models_e2e.py`. Both already have it on `main` (upstream), so they are not counted. A grep of every `from __future__` in `backend/` (excluding venv), checked against `main`, finds no file this branch introduced.

### 90913de regression scan

The diff deletes the four import lines (plus a blank line each), rewrites the two doc lines, and adds evidence files and one handoff line. There are no other code changes. The rewritten doc lines match `resolve_engine_availability` (base.py:304-336) and `DownloadConfirmDialog.tsx:66-67`. No regressions found.

### Branch-wide Style and constitution scan (`git diff main...HEAD`, added lines only)

| Check | Scope | Result |
|---|---|---|
| `from __future__ import annotations` | 25 added or changed `.py` files under backend/, plus the 2 probe scripts | None introduced |
| `typing.List/Dict/Optional/Union/Tuple`, `List[`/`Optional[` in new code | Same | None introduced. `base.py`'s `from typing import … List, Optional, Tuple` is on `main`; the branch only adds `TYPE_CHECKING`. `models.py:93`'s `Optional[str]` is an existing line whose pattern was widened. |
| Wildcard imports | Same | None |
| Heavy imports lazy and marked | backends, services, routes, utils, models.py | Two new heavy imports, both inside functions and marked `# lazy: heavy import` (base.py torch, voxcpm_backend.py voxcpm) |
| Relative imports within `backend` | Package code | New package code uses relative imports. The new tests use `from backend.…`, the existing test convention on `main` (e.g. test_f5tts_backend.py:20) and allowed by the style guide's example. Not counted. |
| Engine-name capability branching (Principle III) | Hand-written app/src (generated api/ excluded) | Only `useGenerationForm.ts:154` and `:176`: engine to model-name and display-name mapping, in the same chain every engine uses. That is registration, not capability. No other `=== 'voxcpm'` |
| `any`, `@ts-ignore`, default exports | Hand-written app/src | None. One justified `biome-ignore` (a11y anchor filled by `Trans`, the FR-025 link). The `any`/`@ts-ignore` hits are in generated `app/src/lib/api/core`, which must not be hand-edited. |
| Telemetry or remote calls (Principle I) | backend/ added lines | None |
| CI runner policy | `.github/workflows/release.yml`, `Dockerfile`, `justfile` | No `runs-on` line changed |
| Debug `print` | backend/ added lines | Only in `test_all_models_e2e.py` (a manual E2E runner) and in code strings for test subprocesses. Not counted. |

Nothing in this scan was introduced by the branch in breach of the Style rule or the constitution.

## Counts

- Original 13: 13 resolved.
- N1–N7: 7 resolved.
- P1–P2: 2 resolved.
- Open now: CRITICAL 0 · HIGH 0 · MEDIUM 0 · LOW 0

Blocking: 0

## Files read (all passes)

- **Spec folder:** spec.md, plan.md, research.md, data-model.md, quickstart.md, tasks.md, checklists/requirements.md, contracts/engine-capabilities.md, contracts/tts-backend-protocol.md, workflow/handoff.md (via diff)
- **Revalidation evidence:** docs-fixes.txt, N-docs-fixes.txt, T044-directml-xpu-superseded.md, N4-ruff.txt
- **Constitution and style:** .specify/memory/constitution.md; backend/STYLE_GUIDE.md:51
- **Commit diffs:** `git show` of 699f362, 4b72fed, 39cdc3a (stat), d0a0528
- **Backend engine and registry:** backend/backends/__init__.py (40-153, 485-554, 717-726); backend/backends/voxcpm_backend.py; backend/backends/base.py:225-374
- **Backend services and routes:** backend/services/generation.py (grep, 396-500); backend/services/export_import.py; backend/services/profiles.py:228-240 (grep); backend/routes/models.py; backend/routes/speak.py:20-80; backend/routes/effects.py (grep); backend/routes/transcription.py:50-58
- **Backend support files:** backend/utils/audio.py (grep); backend/models.py (grep); backend/requirements.txt (grep)
- **Backend tests:** backend/tests/test_engine_load_failure.py:1-40 plus the test list; `__future__` grep over backend/ (excluding venv) and against `main`
- **App:** app/src/lib/constants/languages.ts; app/src/components/ServerSettings/ModelManagement.tsx:460-479; engineCapabilityRules.ts and EngineModelSelector.tsx (grep); app/src/lib/api/models/EngineCapabilityResponse.ts header; DefaultService.ts:2278
- **Project docs:** backend/README.md:112-190; CHANGELOG.md:10-63
- **Docs content:** docs/content/docs/developer/{model-management,tts-engines,tts-generation}.mdx (grep and diff); docs/content/docs/overview/{generating-speech,introduction,gpu-acceleration}.mdx; docs/PROJECT_STATUS.md:697-703
- **Build and CI:** justfile, Dockerfile, .github/workflows/release.yml (grep for the voxcpm pin)
